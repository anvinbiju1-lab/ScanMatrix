package com.example.securityapp.ui.theme

import androidx.compose.ui.graphics.Color

// Kinetic Fortress - Surface Architecture
val SurfaceBase      = Color(0xFF0C1321) // Background
val SurfaceLow       = Color(0xFF151B2A) // Secondary Sections
val SurfaceHigh      = Color(0xFF232A39) // Interactive Cards
val SurfaceBright    = Color(0xFF323949) // Floating Modals / Hover

// Kinetic Fortress - Primary & Secondary (Active Intelligence)
val Primary          = Color(0xFF4DA3FF) // Electric Blue
val PrimaryFixedDim  = Color(0xFFA2C9FF) // Light Blue Accent
val Secondary        = Color(0xFF54D8E8) // Cyan / Node Active

// Kinetic Fortress - Functional Colors
val Success          = Color(0xFF45DFA4) // Tertiary
val Danger           = Color(0xFFFFB4AB) // Error Red
val Warning          = Color(0xFFFBBF24) // Amber (Medium Risk)

// On-Colors & Text
val OnSurface        = Color(0xFFDCE2F6) // Light Gray-Blue
val OnSurfaceVariant = Color(0xFFAFC2D9) // Muted Gray-Blue
val OnPrimary        = Color(0xFF001C38) // Dark Blue for text on Primary
val OnSecondary      = Color(0xFF00363C) // Dark Teal for text on Secondary

// Gradients (Kinetic Fortress Rule)
val PrimaryGradient = listOf(Color(0xFF4DA3FF), Color(0xFFA2C9FF))
val SecondaryGradient = listOf(Color(0xFF4da3ff), Color(0xFF54d8e8))
